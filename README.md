run images in docker

<!-- all file in sandbox -->
server image:-  docker build -t sandbox:latest .
router image:-  docker build -t router:latest .
agent image:-   docker build -t agent:latest .
tamplate image:-    docker build -t tamplate:latest .